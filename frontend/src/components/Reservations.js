import React from "react";

const Reservations = () => {
  return (
    <div>
      <h2>My Reservations</h2>
      <p>You don’t have any active reservations right now. Check back later!</p>
    </div>
  );
};

export default Reservations;
